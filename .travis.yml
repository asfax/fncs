language: python
python:
  - "3.7.0"
cache: pip
install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov
  - pip install coveralls
  - pip install black
script:
  - python3 test.py
  - black --check Code/
  
after_success:
  - coveralls
